// The edge of a resizable panel that carries its drag handle. Dragging the
// handle away from the panel grows it, toward the panel shrinks it.
export const enum ResizeEdge {
  Left = 'left',
  Right = 'right',
  Top = 'top',
  Bottom = 'bottom',
}
