// Shared between the Mastra agent registry key and CopilotKit's `agentId` prop — no compiler link between them otherwise.
export const WEATHER_AGENT_ID = 'weatherAgent';
export const SUPERVISOR_AGENT_ID = 'supervisorAgent';
export const SUPPORT_AGENT_ID = 'supportAgent';
export const ANALYZE_INPUT_AGENT_ID = 'analyzeInputAgent';
export const BUILD_RELEASE_AGENT_ID = 'buildReleaseAgent';
